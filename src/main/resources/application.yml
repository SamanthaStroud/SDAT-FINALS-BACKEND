spring:
  application:
    name: backend

  datasource:
    url: jdbc:postgresql://localhost:5433/sdat_finals
    username: sdat
    password: sdat_dev_pw
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    open-in-view: false

server:
  port: 8080
